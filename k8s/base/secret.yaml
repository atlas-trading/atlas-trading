apiVersion: v1
kind: Secret
metadata:
  name: atlas-secrets
  namespace: atlas-trading
type: Opaque
stringData:
  # Database credentials
  DB_USER: "atlas"
  DB_PASSWORD: "changeme_in_production"

  # Redis password (optional)
  REDIS_PASSWORD: ""

  # API keys (if needed)
  SECRET_KEY: "changeme_super_secret_key_for_jwt"
